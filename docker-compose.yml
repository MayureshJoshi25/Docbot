version: '3.8'

services:
  redis:
    image: redis:6.2-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  app:
    build: .
    ports:
      - "8501:8501"
    environment:
      REDIS_URL: "redis://redis:6379/0"
    depends_on:
      - redis

volumes:
  redis_data:
